import PagePlaceholder from "@/components/PagePlaceholder";

export default function PricingPage() {
  return (
    <PagePlaceholder
      title="Pricing"
      phase={2}
      description="Audit, Build, Membership, and Enterprise pricing, with an ROI calculator."
      bullets={["Starting price per tier","Enterprise custom-pricing contact","ROI calculator widget"]}
    />
  );
}
