import PagePlaceholder from "@/components/PagePlaceholder";

export default function BookCallPage() {
  return (
    <PagePlaceholder
      title="Book a Discovery Call"
      phase={2}
      description="Discovery call booking page with calendar integration."
      bullets={["Calendar embed","Pre-call qualifying questions"]}
    />
  );
}
