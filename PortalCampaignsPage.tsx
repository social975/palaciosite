import PagePlaceholder from "@/components/PagePlaceholder";

export default function PortalCampaignsPage() {
  return (
    <PagePlaceholder
      title="Campaigns"
      phase={2}
      description="Palacio Run campaign briefs, progress, KPIs, and downloadable reports."
      bullets={["Campaign briefs","KPI tracking","Downloadable reports"]}
    />
  );
}
